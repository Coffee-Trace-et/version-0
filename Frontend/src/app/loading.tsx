"use client";
import Loader from "./(DashboardLayout)/components/Loder/Loder";
const Loading = () => {
  return (
    <div className="flex items-center justify-center h-screen">
      <Loader />
    </div>
  );
};

export default Loading;
